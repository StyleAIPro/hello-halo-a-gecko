/**
 * Claude Agent SDK version constant
 *
 * This is the single source of truth for the SDK version used across the project.
 * When upgrading the SDK, update this constant AND both package.json files:
 *   - /package.json
 *   - /packages/remote-agent-proxy/package.json
 */
export const CLAUDE_AGENT_SDK_VERSION = '0.2.104';
